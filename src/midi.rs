/// The common submodule contains common structures and traits used by the other submodules.
pub mod common;

/// The midi submodule contains a structure that represents a MIDI file.
pub mod processor;

/// The track submodule contains a structure that represents a MIDI track.
pub mod track;
